<%= form_for @task do |f| %>
  <div>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
  <div>
    <%= f.submit 'Save', class: 'btn btn-primary' %>
  </div>
<% end %>

<p>
  <% if @task.errors.any? %>
    <%= flash[:notice]%>
  <% end %>
</p>