<h1>Tasks</h1>

<table>
  <thead>
    <th>Id</th>
    <th>Description</th>
  </thead>
  <tbody>
    <% @tasks.each do |task| %>
    <tr>
      <td> <%= task.id %> </td>
      <td> <%= link_to task.description, task_path(task.id) %> </td>
      <td> <%= button_to 'Delete', task_path(task.id), method: :delete %> </td>
    </tr>
    <% end %>
  </tbody>
</table>